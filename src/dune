(library
 (public_name leveldb)
 (wrapped false)
 (foreign_stubs
  (language cxx)
  (flags (:standard -lleveldb -std=gnu++11))
  (names leveldb_stubs))
 (flags
  :standard
  (:include flags.sexp))
 (libraries threads))

(rule
 (targets flags.sexp)
 (action
  (run ./config/discover.exe)))
